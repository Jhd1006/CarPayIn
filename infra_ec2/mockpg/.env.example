# EC2 배포용 민감 환경변수
# 이 파일을 참고해 .env 를 만들되, .env 는 절대 커밋하지 마세요.
# CI/CD가 GitLab 변수(MOCKPG_DATABASE_URL, MOCKPG_WEBHOOK_SECRET)로
# /home/ubuntu/deploy/.env 를 자동 생성합니다.

MOCK_PG_DATABASE_URL=postgresql+psycopg://mockpg:<password>@10.1.130.39:5432/mockpgdb
PG_WEBHOOK_SECRET=<pg-webhook-secret>
